<table class="table-bordered table table-condensed">
	<thead>
		<tr>
			<th>Name</th>
			<th>URL</th>
			<th>Status</th>
		</tr>
	</thead>
	<tbody>
<%
	sites.forEach(function(site, i) {
		var status = Math.round(site.status / 100);
%>
		<tr>
			<td><%- site.name %></td>
			<td><%- site.url %></td>
			<td>
				<span class="badge badge-<%- (status == 2 ? 'success' : ( [3,4].indexOf(status) !== -1 ? 'warning' : isNaN(status) ? '' : 'danger' ) ) %>">
					<%- site.status || 'N/A' %>
				</span>
			</td>
		</tr>
<%
	})
%>
	</tbody>
</table>